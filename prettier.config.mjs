/** @type {import('prettier').Config} */
const config = {
  plugins: ["prettier-plugin-organize-imports"],
  bracketSpacing: true,
  semi: true,
  trailingComma: "all",
  printWidth: 80,
  tabWidth: 2,
};

export default config;
